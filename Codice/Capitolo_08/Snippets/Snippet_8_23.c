#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    struct date_V2 // una struttura di tipo struct date_V2
    {
        int day; // 32 bit 
        int month; // 32 bit
        int year; // 32 bit
        bool isLeapYear; // 8 bit
    };
    
    return (EXIT_SUCCESS);
}

