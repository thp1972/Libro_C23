#include <stdio.h>
#include <stdlib.h>

/* prototipo della funzione subtraction */
int subtraction(int data[]);

int main(void)
{
    return (EXIT_SUCCESS);
}

/* definizione della funzione subtraction */
int subtraction(int data[])
{
}
