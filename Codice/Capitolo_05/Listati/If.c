/* If.c :: Uso dell'istruzione if :: */
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int a = -1;

    // a è minore di 10? 
    if (a < 10)
        printf("a < 10\n");

    return (EXIT_SUCCESS);
}