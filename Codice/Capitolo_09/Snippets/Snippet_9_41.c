#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // un oggetto di tipo struttura; una struttura di tipo struct data
    struct data 
    {
        int code;
        float ratio;        
    };

    return (EXIT_SUCCESS);
}